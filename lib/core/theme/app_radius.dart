abstract class AppRadius {
  // Binary system per DESIGN.md:
  // 12px for containers (cards, code blocks, panels)
  static const double container = 12.0;

  // 9999px for all interactive elements (buttons, tabs, inputs, tags, badges)
  static const double pill = 9999.0;
}
